// Build metadata. __APP_VERSION__ and __BUILD_TIME__ are replaced at build time
// by Vite's `define` (see vite.config.js); they are also defined for `vite dev`.
export const appVersion = __APP_VERSION__;
export const buildTime = __BUILD_TIME__;
